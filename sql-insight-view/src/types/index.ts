export interface FixItem {
    name: string,
    length?: number,
    topOffset?: string,
    bottomOffset?: string,
    effect?: number,
    dialogComponent: object
}


export interface InnodbPageItem {
    length: number,
    name: string,
    desc: string
}

